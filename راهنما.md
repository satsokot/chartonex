# ChartoneX — استخراج قیمت تتر از تلگرام

## نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.9 یا بالاتر
- ویندوز 10/11

### نصب کتابخانه‌ها
```bash
pip install -r requirements.txt
```

### دریافت API تلگرام
1. به سایت [my.telegram.org](https://my.telegram.org) مراجعه کنید
2. با شماره تلفن خود وارد شوید
3. گزینه **API development tools** را انتخاب کنید
4. یک اپلیکیشن جدید بسازید
5. **API ID** و **API Hash** را کپی کنید

### اجرای برنامه
```bash
python telegram_price_scraper.py
```

## نحوه استفاده

### ۱. تنظیمات
- صفحه «تنظیمات» را باز کنید
- API ID، API Hash و شماره تلفن خود را وارد کنید
- ذخیره کنید

### ۲. افزودن کانال‌ها
- صفحه «کانال‌ها» را باز کنید
- نام و لینک کانال‌های تلگرام را وارد کنید
- فرمت‌های قابل قبول: `@channel_name` یا `https://t.me/channel_name`

### ۳. استخراج قیمت
- صفحه «استخراج» را باز کنید
- تعداد پیام‌های مورد بررسی را تنظیم کنید
- دکمه «شروع استخراج» را بزنید
- در اولین اجرا، کد تأیید تلگرام در ترمینال درخواست می‌شود

### ۴. مشاهده خروجی
- نتایج در صفحه «خروجی» نمایش داده می‌شوند
- بهترین قیمت خرید و فروش در بالا نمایش داده می‌شود
- امکان خروجی CSV وجود دارد

## فرمت‌های پشتیبانی‌شده برای استخراج قیمت

برنامه پیام‌هایی با این الگوها را تشخیص می‌دهد:
- `خرید: 98500` / `فروش: 99000`
- `buy: 98500` / `sell: 99000`
- `قیمت خرید تتر: 98500`
- `خرید ← 98500`

## دیتابیس
- کانال‌ها در فایل `channels.json` ذخیره می‌شوند
- تنظیمات در فایل `settings.json` ذخیره می‌شوند
- فایل session تلگرام با نام `chartonex_session.session` ایجاد می‌شود
